import React, { Fragment } from "react";

const divStyle = {
  background: "#aaa"
};

const About = () => {
  return (
    <Fragment>
      <div className='two-column' style={divStyle}>
        <div></div>
      </div>
    </Fragment>
  );
};

export default About;
